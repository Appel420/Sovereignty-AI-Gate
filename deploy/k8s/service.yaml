apiVersion: v1
kind: Service
metadata:
  name: sia-dashboard
  labels:
    app: sia-dashboard
spec:
  selector:
    app: sia-dashboard
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
